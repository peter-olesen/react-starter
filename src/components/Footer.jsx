import style from '../styles/Footer.module.scss'

export const Footer = () => {
    return (
        <footer className={style.footerStyle}>
            <p className={style.footStuckinToilet}>I'm a paragraph inside the footer</p>
        </footer>
    )
}