import style from '../styles/Main.module.scss'

export const Main = () => {
    return (
        <main className={style.mainStyle}>
            <p className={style.mainParagraphStyle}>Hello from Main</p>
        </main>
    )
}